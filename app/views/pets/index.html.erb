<h1>Found pets</h1>

<ul>
  <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet.id) %> <%= link_to 'Remove', pet_path(pet.id), data: { turbo_method: :delete, turbo_confirm: 'WHY?!?!?!?!'} %></li>
  <% end %>
</ul>

<%= link_to 'Report pet', new_pet_path %>
